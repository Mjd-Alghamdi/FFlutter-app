import 'package:flutter/material.dart';

class MjdView extends StatelessWidget {
  const MjdView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Hi from the new view"),
      ),
    );
  }
}
